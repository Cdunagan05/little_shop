<h1>All Items</h1>

<% @items.each do |item| %>
  <div class='container'>
    <div class='row'>
      <%= render partial: 'shared/item_list', locals: { item: item } %>
    </div>
    <div class='container'>
      <br/><%= cart_button(item) %><br/>
    </div>
  </div>
<% end %>
