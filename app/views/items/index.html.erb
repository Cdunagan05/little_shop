<h1>All Items</h1>

<% @items.each do |item| %>
  <div>
    <%= render partial: 'shared/item_list', locals: { item: item } %>
    <%= button_to 'Add to Cart', cart_items_path(item_id: item.id), class: 'btn btn-primary' %>
  </div>
<% end %>
